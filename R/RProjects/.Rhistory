yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
sets_yr <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$cnt[num-1]
sets_yr$set_num[j] <- yrs_dur$set[num]
}
View(sets_yr)
match(8,yrs_dur$set)
yrs_dur[138,]
yrs_dur[137,]
View(yrs_dur)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
sets_yr <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$cnt[num-1]+1
sets_yr$set_num[j] <- yrs_dur$set[num]
}
View(sets_yr)
meanbyyr <- aggregate(sets_yr$num_days, list(sets_yr$year_val),
FUN = mean, na.rm=TRUE)
meanbyyr
colnames(meanbyyr) <- c("Year", "num_mean")
dl16 <- mean(meanbyyr$num_mean)
sl16
dl16
medianflow <- median(qfiletempf$discharge,na.rm=TRUE)
day7mean <- rollmean(qfiletempf$discharge, 7, align = "right",
na.pad = TRUE)
day7rollingavg <- data.frame(qfiletempf, day7mean)
rollingavgs7day <- subset(day7rollingavg, day7rollingavg$day7mean !=
"NA")
max7daybyyear <- aggregate(rollingavgs7day$day7mean,
list(rollingavgs7day$year_val), max, na.rm=TRUE)
meanmax7day <- mean(max7daybyyear$x)
dh12 <- meanmax7day/medianflow
dh12
medianflow <- median(qfiletempf$discharge,na.rm=TRUE)
day30mean <- rollmean(qfiletempf$discharge, 30, align = "right",
na.pad = TRUE)
day30rollingavg <- data.frame(qfiletempf, day30mean)
rollingavgs30day <- subset(day30rollingavg, day30rollingavg$day30mean !=
"NA")
max30daybyyear <- aggregate(rollingavgs30day$day30mean,
list(rollingavgs30day$year_val), max, na.rm=TRUE)
meanmax30day <- mean(max30daybyyear$x)
dh13 <- meanmax30day/medianflow
dh13
meanmonthly <- aggregate(qfiletempf$discharge, list(qfiletempf$month_val), mean, na.rm=TRUE)
meanflow <- mean(meanmonthly$x)
isolateq <- meanmonthly$x
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.05))
hfcrit <- sortq[frank]
dh14 <- hfcrit/meanflow
return(dh14)
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.25))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
sets_yr <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$cnt[num-1]+1
sets_yr$set_num[j] <- yrs_dur$set[num]
}
meanbyyr <- aggregate(sets_yr$num_days, list(sets_yr$year_val),
FUN = mean, na.rm=TRUE)
colnames(meanbyyr) <- c("Year", "num_mean")
dh15 <- median(meanbyyr$num_mean)
dh16 <- sd(meanbyyr$num_mean)/dh15
dh15.16 <- list(dh15=dh15,dh16=dh16)
dh15.16
lfcrit <- median(qfiletempf$discharge)
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
sets_yr <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$cnt[num-1]+1
sets_yr$set_num[j] <- yrs_dur$set[num]
}
meanbyyr <- aggregate(sets_yr$num_days, list(sets_yr$year_val),
FUN = mean, na.rm=TRUE)
colnames(meanbyyr) <- c("Year", "num_mean")
dh15 <- mean(meanbyyr$num_mean)
dh15
lfcrit <- median(qfiletempf$discharge)*7
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
sets_yr <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$cnt[num-1]+1
sets_yr$set_num[j] <- yrs_dur$set[num]
}
meanbyyr <- aggregate(sets_yr$num_days, list(sets_yr$year_val),
FUN = mean, na.rm=TRUE)
colnames(meanbyyr) <- c("Year", "num_mean")
dh19 <- mean(meanbyyr$num_mean)
dh19
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.25))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
sets_yr <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$cnt[num-1]+1
sets_yr$set_num[j] <- yrs_dur$set[num]
}
dh20 <- mean(sets_yr$num_days)
dh20
lfcrit
frank <- floor(findrank(length(sortq), 0.75))
lfcrit <- sortq[frank]
lfcrit
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.75))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.40))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
str(subset_crit)
num_year <- aggregate(qfiletempf$discharge, list(qfiletempf$year_val), count, na.rm=TRUE)
num_year <- aggregate(qfiletempf$discharge, list(qfiletempf$year_val), length, na.rm=TRUE)
num_year <- aggregate(qfiletempf$discharge, list(qfiletempf$year_val), nrow, na.rm=TRUE)
num_year <- aggregate(qfiletempf$discharge, list(qfiletempf$year_val), nrow)
View(num_year)
num_year <- aggregate(subset_crit$discharge, list(subset_crit$year_val), nrow)
View(num_year)
num_year <- aggregate(subset_crit$discharge, list(subset_crit$year_val), function(x) sum(!is.na(x)))
View(num_year)
View(num_year)
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.40))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
num_year <- aggregate(subset_crit$discharge, list(subset_crit$year_val), function(x) sum(!is.na(x)))
dh23 <- mean(num_year$x)
dh23
str(qfiletempf)
sets_cnt <- max(yrs_dur$set)
days_btwn <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
num2 <- match(j-1,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
sets_yr$set_num[j] <- yrs_dur$set[num]
}
j
for (j in 2:sets_cnt) {
num <- match(j,yrs_dur$set)
num2 <- match(j-1,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
sets_yr$set_num[j] <- yrs_dur$set[num]
}
j
num
num2
View(yrs_dur)
num <- match(j,yrs_dur$set)
num2 <- match(j-1,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
yrs_dur$date[num-1]
num-1
yrs_dur$date[4]
yrs_dur$date[4,]
yrs_dur$date[,4]
View(yrs_dur)
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.40))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)), date = rep(sysdate,nrow(subset_crit)))
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)), date = rep(sys.date(),nrow(subset_crit)))
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)), date = rep(Sys.date(),nrow(subset_crit)))
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)), date = rep(Sys.Date(),nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
yrs_dur$date[i+1] <- subset_crit$date[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
days_btwn <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 2:sets_cnt) {
num <- match(j,yrs_dur$set)
num2 <- match(j-1,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
sets_yr$set_num[j] <- yrs_dur$set[num]
}
j
nrow(yrs_dur)
max(yrs_dur$set)
num <- match(j,yrs_dur$set)
num2 <- match(j-1,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
num
yrs_dur$year_val[num-1]
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$year_val[j] <- 2010
str(sets_yr)
sets_yr$year_val[955]
yrs_dur$year_val[num-1]
sets_cnt
View(sets_yr)
j
num
num2
nrow(sets_yr)
days_btwn <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 2:sets_cnt) {
num <- match(j,yrs_dur$set)
num2 <- match(j-1,yrs_dur$set)
days_btwn$year_val[j] <- yrs_dur$year_val[num-1]
days_btwn$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
days_btwn$set_num[j] <- yrs_dur$set[num]
}
View(days_btwn)
days_btwn <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j+1,yrs_dur$set)
num2 <- match(j,yrs_dur$set)
days_btwn$year_val[j] <- yrs_dur$year_val[num-1]
days_btwn$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
days_btwn$set_num[j] <- yrs_dur$set[num]
}
dh22 <- mean(num_year$x)
dh2
dh22
View(days_btwn)
sets_cnt <- max(yrs_dur$set)-1
days_btwn <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j+1,yrs_dur$set)
num2 <- match(j,yrs_dur$set)
days_btwn$year_val[j] <- yrs_dur$year_val[num-1]
days_btwn$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
days_btwn$set_num[j] <- yrs_dur$set[num]
}
medbyyr <- aggregate(days_btwn$num_days, list(days_btwn$year_val), median, na.rm=TRUE)
dh22 <- mean(medbyyr$x)
dh22
medbyyr
days_btwn
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.40))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)), date = rep(Sys.Date(),nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
yrs_dur$date[i+1] <- subset_crit$date[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)-1
days_btwn <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j+1,yrs_dur$set)
num2 <- match(j,yrs_dur$set)
days_btwn$year_val[j] <- yrs_dur$year_val[num-1]
days_btwn$num_days[j] <- yrs_dur$date[num-1]-yrs_dur$date[num2-1]
days_btwn$set_num[j] <- yrs_dur$set[num]
}
medbyyr <- aggregate(days_btwn$num_days, list(days_btwn$year_val), median, na.rm=TRUE)
dh22 <- mean(medbyyr$x)
dh22
countbyyr <- aggregate(sets_yr$set_num, list(sets_yr$year_val), function(x) sum(!is.na(x)))
countbyyr
sum(countbyyr$x)
isolateq <- qfiletempf$discharge
sortq <- sort(isolateq)
frank <- floor(findrank(length(sortq), 0.4))
lfcrit <- sortq[frank]
subset_crit <- subset(qfiletempf, qfiletempf$discharge>lfcrit)
yrs_dur <- data.frame(year_val = rep(0,nrow(subset_crit)), cnt = rep(9,nrow(subset_crit)), set = rep(0,nrow(subset_crit)))
yrs_dur$cnt[1] <- 0
yrs_dur$year_val[1] <- subset_crit$year_val[1]
yrs_dur$set[1] <- 0
i <- 1
counter <- 0
set_cnt <- 1
sub_length <- nrow(subset_crit)-1
for (i in 1:sub_length) {
yrs_dur$year_val[i+1] <- subset_crit$year_val[i+1]
if (subset_crit$jul_val[i+1] - subset_crit$jul_val[i] == 1) {
yrs_dur$cnt[i+1]<-1+counter
counter <- counter+1
}
else {
yrs_dur$cnt[i+1]<-0
counter <- 0
yrs_dur$set[i+1]<-set_cnt
set_cnt <- set_cnt+1
}
}
sets_cnt <- max(yrs_dur$set)
sets_yr <- data.frame(year_val = rep(0,sets_cnt), num_days = rep(0,sets_cnt), set_num = rep(0,sets_cnt))
for (j in 1:sets_cnt) {
num <- match(j,yrs_dur$set)
sets_yr$year_val[j] <- yrs_dur$year_val[num-1]
sets_yr$num_days[j] <- yrs_dur$cnt[num-1]+1
sets_yr$set_num[j] <- yrs_dur$set[num]
}
countbyyr <- aggregate(sets_yr$set_num, list(sets_yr$year_val), function(x) sum(!is.na(x)))
fh11 <- mean(countbyyr$x)
fh11
# Build the package:
library(devtools)
library(devtools)
setwd("C:/Users/jlthomps/Desktop/git/USGS-NWC/R/RProjects/")
load_all("HITHATStats/",reset = TRUE)
